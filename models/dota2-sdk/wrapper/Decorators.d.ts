// AUTO-GENERATED - do not edit.
/**
 * Every registered modifier class by name. The registration below is the only way into the shared
 * map from this SDK, so its values are always Dota `Modifier` subclasses.
 * @example
 * const constructor = ModifierClasses.get("modifier_bloodseeker_thirst")
 */
/**
 * Names registered through {@link WrapperClassModifier} that have not been seen in the
 * `ModifierNames` string table yet. A name still here once the table arrives is a typo.
 */
/**
 * Binds a Modifier subclass to the modifier name the server sends. Unlike the shared registration
 * this also guards against a name being claimed twice and records it for the string-table check.
 * @example
 * @WrapperClassModifier()
 * export class modifier_bloodseeker_thirst extends Modifier {}
 */
