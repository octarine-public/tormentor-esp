// AUTO-GENERATED - do not edit.
declare namespace MenuSDK {
	/**
	 * Adds the Animations page under Settings, one toggle per motion the menu
	 * owns: the first-launch intro, the window opening and closing (with its
	 * effect and duration), the navigation rail folding, a tab revealing its
	 * rows, the tab hover arrow and the on-screen hotkeys panel. Each drives its
	 * `MenuFlags` switch, so every animation the menu plays is turned off from
	 * the menu itself.
	 */
	function SetupAnimationsMenu(): void
}
